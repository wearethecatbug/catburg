import type {AnimationNameMap} from '../../systems/animation';

export const DOG_ANIMATION_NAME_MAP: AnimationNameMap = {
    Idle: 'Idle',
    Walk: 'Walk',
    Attack: 'Attack',
} satisfies AnimationNameMap;
